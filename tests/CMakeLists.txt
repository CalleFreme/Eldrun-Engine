# Add test targets here once a framework is chosen.
# Example future layout:
# add_executable(eldrun_core_tests ...)
# target_link_libraries(eldrun_core_tests PRIVATE eldrun_core)
# add_test(NAME eldrun_core_tests COMMAND eldrun_core_tests)